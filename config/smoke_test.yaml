# Lightweight configuration – finishes <30 s on T4/16 GB, 500 MB RAM

seed: 0
streaming: true
n_samples: 128  # per split

model:
  name: sentence-transformers/all-MiniLM-L6-v2
  lr: 5.0e-5
  epochs: 1
  batch_size: 8
  unfreeze_last_n: 1

dataset: openai/gsm8k
